# UI components. Deliberately empty: importing .main here pulled tkinter
# and every other UI module in behind `from emt_ui import palette`, which
# is the one module meant to import in a bare interpreter. Every consumer
# uses the submodule path already.
